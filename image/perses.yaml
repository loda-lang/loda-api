# Perses configuration file
database:
  file:
    folder: /var/lib/perses
    extension: json

security:
  # Enable anonymous access for read-only operations
  enable_auth: false
  authorization:
    guest_permissions:
      - actions:
          - read
        scopes:
          - "*"
